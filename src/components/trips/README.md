# components/trips

Reserved for the trip builder UI (Phase 2+): day-by-day itinerary editor,
trip item cards, guest-vs-account state indicator.

Data/state comes from `src/features/trips` (account trips) and
`src/lib/guest` (local guest trips, merged on sign-in). Built on
`src/components/ui`.
